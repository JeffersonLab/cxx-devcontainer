services:
  #TODO: Your other containers go here!

  dev:
    image: jeffersonlab/cxx-devcontainer:latest
    hostname: dev
    container_name: dev
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock